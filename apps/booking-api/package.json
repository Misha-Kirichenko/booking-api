{
  "name": "@booking-api/booking-api",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "name": "booking-api",
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "webpack-cli build",
          "args": [
            "node-env=production"
          ]
        },
        "configurations": {
          "development": {
            "args": [
              "node-env=development"
            ]
          }
        }
      },
      "serve": {
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "booking-api:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "booking-api:build:development"
          },
          "production": {
            "buildTarget": "booking-api:build:production"
          }
        }
      }
    }
  }
}
